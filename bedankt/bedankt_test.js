'use strict';

describe('myApp.bedankt module', function() {

  beforeEach(module('myApp.bedankt'));

  describe('bedankt controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bedanktCtrl = $controller('bedanktCtrl');
      expect(bedanktCtrl).toBeDefined();
    }));

  });
});