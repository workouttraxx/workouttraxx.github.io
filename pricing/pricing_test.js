'use strict';

describe('myApp.pricing module', function() {

  beforeEach(module('myApp.pricing'));

  describe('pricing controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pricingCtrl = $controller('pricingCtrl');
      expect(pricingCtrl).toBeDefined();
    }));

  });
});