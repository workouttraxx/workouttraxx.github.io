'use strict';

describe('myApp.fastsignup module', function() {

  beforeEach(module('myApp.fastsignup'));

  describe('fastsignup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var fastsignupCtrl = $controller('fastsignupCtrl');
      expect(fastsignupCtrl).toBeDefined();
    }));

  });
});