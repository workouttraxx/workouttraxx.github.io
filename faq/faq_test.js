'use strict';

describe('myApp.faq module', function() {

  beforeEach(module('myApp.faq'));

  describe('faq controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var faqCtrl = $controller('faqCtrl');
      expect(faqCtrl).toBeDefined();
    }));

  });
});